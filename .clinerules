CRITICAL RULE FOR ALL FUTURE AI AGENTS:
1. Docs Update: You MUST update README.md and/or docs/decisions/ (ADRs) if you alter the architecture.
2. GUI Contract: NEVER alter the exact stdout regex "Saved file name: " or "==== [ FINANCIAL REPORT ] ====" without explicit permission. The legacy Python GUI depends on them.
3. Dual Architecture: Do NOT break the WebController. The system operates as BOTH a CLI tool for Python and a Web Server for index.ejs.
4. Persian ZWNJ: Never use String manipulation that corrupts Zero-Width Non-Joiners (\u200C) or strips <ID:XXX> tags.
